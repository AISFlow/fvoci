; Pack file: dependencies as regular packages. @fvoci/editor and @fvoci/i18n
; live outside this package, so npm's default file: install would only symlink
; them and skip their dependencies (see `npm help install` / `npm help ci`).
install-links=true
